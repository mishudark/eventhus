box: golang

services:
    - mongo

build:
  steps:
    - wercker/setup-go-workspace:
      package-dir: github.com/mishudark/eventhus

    - wercker/glide-install

    - script:
        name: go test
        code: |
          go test
